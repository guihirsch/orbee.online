# Dependências básicas do FastAPI (instaladas com sucesso)
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.11.9
pydantic-settings==2.0.3
email-validator==2.3.0

# Banco de dados e autenticação
supabase==2.20.0
httpx==0.27.2
python-multipart==0.0.6
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-dotenv==1.0.0

# CORS e requests
fastapi-cors==0.0.6
requests==2.31.0

# Dados básicos
numpy>=1.24.0,<2.0.0
pillow>=10.0.0

# Sentinel Hub (já existente)
sentinelhub==3.9.0

# Dependências geoespaciais (instalar separadamente se necessário)
# Para instalar as dependências geoespaciais, use:
# pip install rasterio geopandas rioxarray rio-tiler rio-cogeo pystac-client boto3

# Banco de dados async (opcional)
# SQLAlchemy>=2.0.0
# asyncpg>=0.29.0

# Deep Learning (opcional - instalar separadamente)
# torch