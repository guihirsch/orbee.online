# Dependências da Fase 0 (validação em Colab isolado)
# Uso: no Colab, a Célula 0 instala tudo. Este arquivo documenta/serve para reexecução local.
# Nenhuma destas dependências entra no backend até o veredito VERDE.

pystac-client>=0.7.0
planetary-computer>=1.0.0
stackstac>=0.4.0
rioxarray>=0.15.0
xarray>=2023.1.0
dask>=2023.1.0
geopandas>=0.13.0
osmnx>=1.3.0
shapely>=2.0.0
pyproj>=3.4.0
numpy>=1.24.0
pandas>=1.5.0
matplotlib>=3.6.0
scikit-image>=0.19.0

# Super-resolução open-source (modelos ESA OpenSR) — instalar só antes das Células 3/4
# sen2sr>=0.1.0          # SEN2SRLite (primário, transformer, CPU ok) — https://github.com/ESAOpenSR/sen2sr
# opensr-model>=1.0.0    # LDSR-S2 difusão + incerteza (secundário, requer GPU) — https://github.com/ESAOpenSR/opensr-model
# opensr-utils           # tiling georreferenciado de cenas grandes
# opensr-test            # benchmark oficial (métricas de alucinação/omissão) — gates usam implementação numpy própria como fallback
# mlstac                 # download dos checkpoints via STAC
# cubo                   # construção de datacubes S2 L2A
# torch>=2.0.0           # backend dos modelos (Colab já traz; para LDSR-S2 usar runtime com GPU)
